language: python
python:
  - "3.8"
cache: pip
install:
  - pip install -r requirements.txt
script:
  - python -c "pass"
deploy:
  provider: heroku
  api_key:
    # heroku auth:token
    secure: $HEROKU_TOKEN
    app: rtlog-validator
  on:
    branch: master